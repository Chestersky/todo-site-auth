<% include ./partials/header %>
<div class="ui main text container segment">
    <div class="ui huge header">New Todo</div>
    <form class="ui form" action="/todos" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="todo[title]" placeholder="title">
        </div>
        <div class="field">
            <label>Description</label>
            <textarea name="todo[body]" placeholder="description"></textarea>
        </div>
        <input class="ui black basic button" type="submit" value="Send">
    </form>
</div>
<% include ./partials/footer %>